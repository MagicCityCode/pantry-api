import express from "express";
// import user from "./controller/user"
const router = express.Router();
// // Get from /:id?
// router.get("/:id?", user.handleUserGet);
// // Post to /
// router.post("/", user.handleUserPost);
// // Put to /:id
// router.put("/:id", user.handleUserPut);
// // Delete from /:id
// router.delete("/:id", user.handleUserDelete);
export default router;
